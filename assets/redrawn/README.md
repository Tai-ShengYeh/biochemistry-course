# Biochemistry redrawn figures W02-W17 (v2)

Canvas 1672x941, background #FDF8E8.
v2: number markers are placed in EMPTY background near each structure, connected by a thin
grey leader line, and never overlap drawn content. Every topic has both _clean and _labeled;
the labeled file is the clean base plus the marker layer only, so they are pixel-identical
except where markers/leaders sit.
